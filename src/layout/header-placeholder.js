import React from 'react';

const styles = {
    headerReplacement: {
        width: '100%',
        height: 64,
    }
};

const HeaderReplacement = () => (
    <div style={styles.headerReplacement}/>
);

export default HeaderReplacement;
